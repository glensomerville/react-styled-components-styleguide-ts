import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { Wrapper, MediumButton } from 'components';

ReactDOM.render(
  <Wrapper>
    <MediumButton>Press Me</MediumButton>
  </Wrapper>,
  document.getElementById('root')
);
